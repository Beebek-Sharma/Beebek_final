# Frontend .env.example file
# Copy to .env.local before running the application

# API URL
VITE_API_URL=http://localhost:8000/api

# Build configuration
NODE_ENV=development

# If deploying to production, set NODE_ENV=production